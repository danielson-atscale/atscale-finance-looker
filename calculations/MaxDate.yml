unique_name: MaxDate
object_type: metric_calc
label: Max Date
expression: >-
    WITH MaxDate AS (
        SELECT 
            MAX(CreatedDate) AS MaxDate
        FROM factConsolidated
    )
format: '#,###.0;(#,###.0)'
is_hidden: false
